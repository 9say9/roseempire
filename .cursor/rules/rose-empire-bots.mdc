---
description: Rose Empire 3-bot fleet — Cursor Agent + Roo Code (browser-first Sarah, free Ollama)
alwaysApply: true
---

# Rose Empire Bot Fleet

Full agent guide: `AGENTS.md` | Partner setup: `PARTNER_BOT_SETUP.md`

## Bots (free)
| Bot | Command |
|-----|---------|
| Sarah | `py -3 fleet_scraper.py --limit 5 --mission "care homes Manchester UK"` |
| James | `py -3 fleet_ai.py --agent james "Hotel Name Manchester"` |
| Adeel | `py -3 lead_pipeline.py --qualify` |

Orchestrator: `py -3 fleet_orchestrator.py test-all`

## Sarah — multi-source scraper
Sources: **google_maps**, **google_web**, **linkedin**, **reddit**, **facebook** (beta), **instagram** (beta)

```bat
py -3 fleet_scraper.py --list-sources
py -3 fleet_scraper.py --limit 10 --mission "care homes Manchester UK"
py -3 fleet_scraper.py --sources google_maps,linkedin --limit 8
```

Output: `linkedin-outreach/sarah_multi_source_leads.csv`

## UI language fix
Dashboard at http://127.0.0.1:5050 uses UTF-8 English. Garbled symbols were encoding bugs (now fixed).

## Lead quality
`lead_validation.py` rejects fake/OSM duplicate names. Real leads saved to `linkedin-outreach/refined_google_maps_leads.csv`.

## AI
Ollama free: `start_ollama.bat` then James/Sarah AI work. Roo: Provider=Ollama.
