Rose Empire — local lead data (not synced to GitHub)

This folder is created automatically when you run the bots.
CSV files here stay on your PC only — do not commit them.

After running the fleet you will see files like:
  sarah_multi_source_leads.csv   — raw scraper output (Sarah)
  qualified_manchester_leads.csv — qualified leads (Adeel)
  outreach_master.csv            — ready for email + calls
  email_send_log.csv             — send history

Setup: see PARTNER_BOT_SETUP.md in the repo root.
