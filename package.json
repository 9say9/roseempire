{
  "name": "rose-empire-website",
  "private": true,
  "description": "Rose Empire wholesale site — GitHub Pages + Sarah + Stripe checkout",
  "scripts": {
    "serve": "py -3 server.py",
    "deploy": "git push origin main"
  }
}
